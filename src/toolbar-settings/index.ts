// Secondary entry point: `@damiro/stylo/toolbar-settings`. Kept separate from
// the main barrel so the pointer drag-and-drop layer (and its dependency) stays
// off the resolution path of a plain `@damiro/stylo` import.
export { StyloToolbarSettings } from "./StyloToolbarSettings"
export type { StyloToolbarSettingsProps } from "./StyloToolbarSettings"
